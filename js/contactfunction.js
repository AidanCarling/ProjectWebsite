/*console.log('starting function');

const AWS = require('aws-sdk');

exports.handle = function(e, ctx, callback) {
    console.log(e);
    var params = {
        name: e.name,
        email: e.mail,
        subject: e.subject,
        desc: e.message
    };
} */